program PrNurseCall;

uses
  Forms,
  unUtama in 'unUtama.pas' {frmUtama},
  unMonitor in 'unMonitor.pas' {frmmonitor},
  unPath in 'unPath.pas' {frmPath};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TfrmUtama, frmUtama);
  Application.Run;
end.
